name: Deploy nelafrackowiak.com

on:
  push:
    branches: [claude/build-deploy-cloudflare-9o5mnj]
    paths:
      - 'sites/nelafrackowiak.com/**'
      - '.github/workflows/deploy-nelafrackowiak.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: pages-nelafrackowiak-${{ github.ref }}
  cancel-in-progress: true

defaults:
  run:
    working-directory: sites/nelafrackowiak.com

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: sites/nelafrackowiak.com/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Ensure Pages project exists
        uses: cloudflare/wrangler-action@v3
        continue-on-error: true
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: sites/nelafrackowiak.com
          command: pages project create nelafrackowiak-com --production-branch=main

      - name: Deploy to Cloudflare Pages
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: sites/nelafrackowiak.com
          command: pages deploy dist --project-name nelafrackowiak-com --branch=main --commit-dirty=true
